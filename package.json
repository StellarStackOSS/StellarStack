{
  "name": "stellarstack",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "typecheck": "turbo typecheck",
    "db:push": "turbo run db:push",
    "db:migrate": "turbo run db:migrate",
    "dev:infra": "docker compose -f infra/docker/compose.yaml --env-file infra/docker/.env up -d",
    "dev:infra:down": "docker compose -f infra/docker/compose.yaml --env-file infra/docker/.env down"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "prettier-plugin-tailwindcss": "^0.7.2",
    "turbo": "^2.8.17",
    "typescript": "5.9.3"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  }
}
